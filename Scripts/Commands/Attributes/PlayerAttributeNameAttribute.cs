using UnityEngine;

namespace MiGame.Commands
{
    /// <summary>
    /// 玩家属性名称属性标记
    /// 用于标识需要从PlayerAttributeNames配置中读取属性名称的字段
    /// </summary>
    public class PlayerAttributeNameAttribute : PropertyAttribute { }
}
